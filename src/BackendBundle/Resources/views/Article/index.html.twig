{% extends 'base.html.twig' %}

{% block body %}
    <h1>Articles list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Libelle</th>
                <th>Principeactif</th>
                <th>Excipient</th>
                <th>Modeadministration</th>
                <th>Possologie</th>
                <th>Aspect</th>
                <th>Limitecommande</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for article in articles %}
            <tr>
                <td><a href="{{ path('article_show', { 'id': article.id }) }}">{{ article.id }}</a></td>
                <td>{{ article.libelle }}</td>
                <td>{{ article.principeActif }}</td>
                <td>{{ article.excipient }}</td>
                <td>{{ article.modeAdministration }}</td>
                <td>{{ article.possologie }}</td>
                <td>{{ article.aspect }}</td>
                <td>{{ article.limiteCommande }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('article_show', { 'id': article.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('article_edit', { 'id': article.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('article_new') }}">Create a new article</a>
        </li>
    </ul>
{% endblock %}
